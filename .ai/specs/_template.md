# Spec: {文章标题}

| 字段 | 值 |
| ---- | -- |
| 状态 | draft |
| 交付物 | `content/post/{section}/{slug}.md` |
| Hugo url | `{slug}` |
| 标题 | … |
| 分类 | … |

---

## 1. 目的

## 2. 读者

## 3. 核心信息（必须传达）

## 4. 必须包含（验收清单）

- [ ] 标题、url、分类与 Spec 表一致
- [ ] 正文从 `##` 起，无 `#`（MD025）
- [ ] **标签（SDD）**：`original`、`AI-assisted` + 内容标签；**无** `remix`、`reprint`

## 5. 禁止包含（Do NOT）

## 6. 语气与风格（本篇特有）

默认见 [delivery-style.md](../delivery-style.md)（含 **淡化「我」**、默认不加粗）。此处只写 **本篇** 语气、读者、重心、**人称例外**（若有，如 exploration 可保留第一人称）。

## 6b. 格式（本篇例外，可选）

默认见 [delivery-style.md](../delivery-style.md)（**默认不加粗**）。

| 维度 | 要求 |
| ---- | ---- |
| 本篇例外 | 列出允许加粗的短语；未列出则不加粗 |

## 7. 建议结构（大纲）

## 8. 可选 / 后续修订

## 9. 修订流程

1. 改本 Spec → 2. AI 按 Spec 改交付物 → 3. 勾 §4 验收

## 10. 变更记录

| 日期 | 变更 |
| ---- | ---- |
| YYYY-MM-DD | 初版 |
