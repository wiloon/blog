# Spec: {文章标题}

| 字段 | 值 |
| ---- | -- |
| 状态 | draft |
| 交付物 | `content/post/{section}/{slug}.md` |
| Hugo url | `{slug}` |
| 标题 | … |
| 分类 | … |

---

## 1. 目的

## 2. 读者

## 3. 核心信息（必须传达）

## 4. 必须包含（验收清单）

- [ ] 标题、url、分类与 Spec 表一致
- [ ] 正文从 `##` 起，无 `#`（MD025）
- [ ] **标签（SDD）**：`original`、`AI-assisted` + 内容标签；**无** `remix`、`reprint`

## 5. 禁止包含（Do NOT）

## 6. 语气与风格

（叙事类 Spec 可另加 **格式** 小节，约束加粗、小标题等；见 [exploration.md](exploration.md) §格式。）

## 6b. 格式（可选）

| 维度 | 要求 |
| ---- | ---- |
| 加粗 | 默认不加粗；不为扫读而强调日期、主题词、清单项等 |
| 小标题 | 用 `###`，不用加粗代替 |
| 例外 | 仅当本节列出允许的术语对比（如「A / B」）时可加粗 |

## 7. 建议结构（大纲）

## 8. 可选 / 后续修订

## 9. 修订流程

1. 改本 Spec → 2. AI 按 Spec 改交付物 → 3. 勾 §4 验收

## 10. 变更记录

| 日期 | 变更 |
| ---- | ---- |
| YYYY-MM-DD | 初版 |
