# AI Agent 工作指南

## 🚨 编辑博客文章强制规则 🚨

### ⚠️ 每次编辑文章必做三件事（缺一不可）：

1. ✅ **更新 date 字段** → 改为当前东八区时间 (UTC+8)
2. ✅ **添加标签** → 必须加 `remix` 和 `AI-assisted` 两个标签
3. ✅ **编辑内容** → 完成实际的文章修改

**❌ 如果只是移动文件位置，不要更新日期和标签！**

---

## 编辑博客文章详细说明

### 更新文章日期
当**编辑**文章内容时，**必须立即**同步更新文章顶部 front matter 中的 `date` 字段为当前日期时间。

**关键规则：**
- ✅ 编辑文章内容 → **必须**更新日期
- ❌ 仅移动文件到其他目录 → **不要**更新日期
- 🕐 时区**必须**使用东八区 (UTC+8)，即 `+08:00`

示例：
```yaml
---
title: 文章标题
author: "-"
date: 2025-10-30T08:30:00+08:00  # 更新为当前时间，使用东八区 (UTC+8)
url: article-url
categories:
  - 分类
tags:
  - 标签1
  - 标签2
---
```

**时区说明：**
- 使用 `+08:00` 表示东八区（北京时间、上海时间等）
- 不要使用 `+00:00` (UTC) 或其他时区
- 格式：`YYYY-MM-DDTHH:MM:SS+08:00`

---

### 添加 AI 辅助标签
当使用 AI 编辑或创建文章后，**必须立即**在同一次操作中添加以下标签到 `tags` 字段：

**标签使用规则：**
- **编辑已有文章**：必须添加 `remix` 和 `AI-assisted` 两个标签
  - `remix` - 表示内容经过重新编辑和改进（先写这个）
  - `AI-assisted` - AI 辅助编辑的标识（后写这个）
- **新建文章**：只添加 `AI-assisted` 标签即可

⚠️ **不要忘记：**
- 编辑文章 = 更新日期 + 添加 `remix` 和 `AI-assisted` 标签
- 新建文章 = 添加 `AI-assisted` 标签

示例：
```yaml
---
title: 文章标题
author: "-"
date: 2025-10-31T08:30:00+08:00  # 使用东八区时间 (UTC+8)
url: article-url
categories:
  - 分类
tags:
  - 原有标签
  - remix          # 内容经过编辑改进
  - AI-assisted    # AI 辅助编辑的标识
---
```

### 编辑工作流程检查清单

每次编辑文章时，按以下顺序检查：

1. ✅ **更新日期**：将 `date` 字段改为当前东八区时间
2. ✅ **添加标签**：确保 `tags` 中包含 `remix` 和 `AI-assisted`
3. ✅ **内容编辑**：完成实际的文章内容修改
4. ✅ **格式检查**：确保 Markdown 格式正确

**记忆口诀：编辑文章 = 改日期 + 加标签 + 改内容**

### 其他注意事项
- 保持文章格式的一致性
- 使用适当的 Markdown 语法
- 代码块要指定语言类型
- 保持中英文之间的适当空格

### Markdown lint 规范（推荐补充）

- 一级标题（#）仅用于文档主标题，正文只出现一次。
- 标题前后需有一个空行。
- 列表项前后需有一个空行，且无多余缩进。
- 有序列表建议全部编号为 1.，如：
  1. 第一项
  1. 第二项
- 无序列表使用 -，左侧不加多余空格。
- 代码块使用三个反引号包裹，并指定语言类型。
- front matter（YAML 区块）与正文之间有一个空行。
- 避免重复标题。
- 避免多余空行（连续空行只保留一个）。
- 保证中英文之间有适当空格。

> 参考 lint 工具：markdownlint、remark-lint

---

## 开发环境信息

### 容器工具
- 本地使用 **nerdctl** 而非 docker 或 podman
- 需要使用 **sudo** 权限执行
- 构建镜像时使用：`sudo nerdctl build`
- 运行容器时使用：`sudo nerdctl run`

### Hugo 博客信息
- 静态站点生成器：Hugo
- 当前主题：PaperMod
- Hugo 版本：Extended 版本（具体版本见 `Containerfile` 中的 `HUGO_VERSION`）
- 内容目录：`content/post/`
- 主题目录：`themes/PaperMod/`
